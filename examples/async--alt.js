// alt

// Providing a means for a fallback or alternative value, alt combines
//two Async instances and will return the first undefined instance
//it encounters or the last undefined instance if it does not
//encounter a undefined instance.

// Async e a ~> Async e a -> Async e a

import log from "./log";
<!-- eslint-disable no-console -->