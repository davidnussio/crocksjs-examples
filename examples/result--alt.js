// alt

// Providing a means for a fallback or alternative value, alt combines
//two Result instances and will return the first undefined it encounters or
//am undefined if neither value is an undefined.

// Result e a ~> Result e a -> Result e a

import log from "./log";
undefined