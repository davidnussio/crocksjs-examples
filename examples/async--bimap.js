// bimap

// Both undefined and undefined values can vary in
//their type, although most of the time, focus on mapping values is placed on
//the undefined portion. When the requirement or need to map
//the undefined portion arises, bimap can be used.

// Async e a ~> ((e -> b), (a -> c)) -> Async b c

import log from "./log";
undefined